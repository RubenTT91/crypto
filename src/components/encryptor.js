function Encryptor(valueText, valueKey) {
  let texto = valueText;
  let llave = valueKey;

let result  = `el Téxto es ${texto}, y la llave es ${llave}`


return result;


}

export default Encryptor;
